export const withCategoryPrefix = (id: string): string =>
  `categorySelector_${id}`;

export const withFrequencyPrefix = (id: string): string =>
  `frequencySelector_${id}`;

export const withNamePrefix = (id: string): string => `nameInput_${id}`;

export const withMovePrefix = (id: string): string => `moveSelector_${id}`;
