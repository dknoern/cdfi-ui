import { LibraryTableColumns } from 'types';

export const tableColumns = [
  LibraryTableColumns.NAME,
  LibraryTableColumns.LAST_MODIFIED,
  LibraryTableColumns.REPORTING_PERIOD,
  LibraryTableColumns.ACTION,
];
